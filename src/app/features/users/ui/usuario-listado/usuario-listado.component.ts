// src/app/features/users/ui/usuario-listado/usuario-listado.component.ts
import { Component } from '@angular/core';

/**
 * Temporary users list placeholder until store & API wiring is done.
 * Replaced in the next feature.
 *
 * @author  Leon. M. Saia
 * @since   2025-08-14
 */
@Component({
  selector: 'app-usuario-listado',
  standalone: true,
  template: `
    <h2>Usuarios</h2>
    <p>Coming next: list + filters + pagination + CRUD.</p>
  `,
})
export class UsuarioListadoComponent {}
